    @model C500Hemis.Models.TbKhoanNopNganSach

@{
    ViewData["Title"] = "Delete";
}
@section Scripts {
    <script src="https://kit.fontawesome.com/5c202d0407.js" crossorigin="anonymous"></script>
}
<h1>Bạn có chắc chắn muốn xóa thông tin khoản thu này</h1>



<div>
    <h4>Khoản nộp ngân sách</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.MaKhoanNop)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.MaKhoanNop)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.TenKhoanNopNganSach)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TenKhoanNopNganSach)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.SoTien)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.SoTien)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.NamTaiChinh)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.NamTaiChinh)
        </dd>
    </dl>

    <div>
        <a class="btn1" id="btn-edit" asp-action="Delete">Xóa</a> |
        <a class="btn1" id="btn-back" asp-action="Index">Back to List</a>
    </div>
</div>
@section Styles {
    <style>

        /* ======= common ====== */
        a {
            text-decoration: none;
        }

        body {
            background-color: #fafbed;
        }

        /* ======== button =========== */
        .buttons {
            display: flex;
            width: 380px;
            gap: 10px;
            --b: 2px; /* the border thickness */
            font-size: 8px;
            --h: 1.6em; /* the height */
        }

            .buttons .button {
                --_c: #FF003C;
                flex: calc(1.25 + var(--_s,0));
                min-width: 0;
                font-size: 40px;
                font-weight: bold;
                height: var(--h);
                cursor: pointer;
                color: var(--_c);
                border: var(--b) solid var(--_c);
                background: conic-gradient(at calc(100% - 1.3*var(--b)) 0,var(--_c) 209deg, #0000 211deg) border-box;
                clip-path: polygon(0 0,100% 0,calc(100% - 0.577*var(--h)) 100%,0 100%);
                padding: 0 calc(0.288*var(--h)) 0 0;
                margin: 0 calc(-0.288*var(--h)) 0 0;
                box-sizing: border-box;
                transition: flex .4s;
            }

                .buttons .button + .button {
                    --_c: #88C100;
                    flex: calc(.75 + var(--_s,0));
                    background: conic-gradient(from -90deg at calc(1.3*var(--b)) 100%,var(--_c) 119deg, #0000 121deg) border-box;
                    clip-path: polygon(calc(0.577*var(--h)) 0,100% 0,100% 100%,0 100%);
                    margin: 0 0 0 calc(-0.288*var(--h));
                    padding: 0 0 0 calc(0.288*var(--h));
                }

                .buttons .button:focus-visible {
                    outline-offset: calc(-2*var(--b));
                    outline: calc(var(--b)/2) solid #000;
                    background: none;
                    clip-path: none;
                    margin: 0;
                    padding: 0;
                }

                    .buttons .button:focus-visible + .button {
                        background: none;
                        clip-path: none;
                        margin: 0;
                        padding: 0;
                    }

                .buttons .button:has(+ .button:focus-visible) {
                    background: none;
                    clip-path: none;
                    margin: 0;
                    padding: 0;
                }

        .button:hover,
        .button:active:not(:focus-visible) {
            --_s: .75;
        }

        .button:active {
            box-shadow: inset 0 0 0 100vmax var(--_c);
            color: #fff;
        }



        body {
            /* display: grid; */
            /* place-content: center; */
            margin: 0;
            height: 100vh;
            font-family: system-ui, sans-serif;
        }

        .btn1 {
            border-radius: 6px;
            box-shadow: rgba(0, 0, 0, 0.1) 1px 2px 4px;
            box-sizing: border-box;
            color: #FFFFFF;
            cursor: pointer;
            display: inline-block;
            font-size: 16px;
            font-weight: 600;
            line-height: 16px;
            min-height: 40px;
            outline: 0;
            padding: 10px 14px;
            text-align: center;
            text-rendering: geometricprecision;
            text-transform: none;
            user-select: none;
            -webkit-user-select: none;
            touch-action: manipulation;
            vertical-align: middle;
        }
        /* ======== edit ======= */
        #btn-edit {
            background: red;
            border: 1px solid blue;
        }

            #btn-edit:hover,
            #btn-edit:active {
                background-color: initial;
                background-position: 0 0;
                color: blue;
            }

            #btn-edit:active {
                opacity: .5;
            }

        /* ======== back ======== */

        #btn-back {
            background: green;
            border: 1px solid green;
        }

            #btn-back:hover,
            #btn-back:active {
                background-color: initial;
                background-position: 0 0;
                color: green;
            }

            #btn-back:active {
                opacity: .5;
            }
    </style>
}